#          Copyright Rein Halbersma 2019-2026.
# Distributed under the Boost Software License, Version 1.0.
#    (See accompanying file LICENSE_1_0.txt or copy at
#          http://www.boost.org/LICENSE_1_0.txt)

name: Consumption

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # No inputs: the three models live under test/consumer, which is where the
  # shared workflow looks.
  consumption:
    uses: rhalbersma/cpp-ci/.github/workflows/consumption.yml@676e6fb7abc784e54a227998bf3e9c39790e63cf # v0.8.0
